# Implementation Plan: kakaotalk_sender

- 작성일: 2026-08-10 (Phase 0 완료 반영: 2026-08-11)
- 작성자: devjan
- 상태: 진행중 (Phase 0 완료)
- 관련 SRS: [`SRS.md`](./SRS.md) — 이 문서는 SRS를 대체하지 않는다. SRS는 "무엇을/왜"
  만드는지, 이 문서는 "어떤 순서로" 만드는지를 다룬다.

## 1. 단계로 나누는 이유
- 이 스킬의 핵심 동작 방식(macOS UI 자동화로 카카오톡 Mac 앱을 조작)은 실제로 검증된 적이
  없었다. 채팅방 목록/검색창/입력창을 접근성 트리에서 어떻게 식별하는지, 클립보드
  붙여넣기가 정상 동작하는지, 전송한 메시지를 다시 읽어 검증할 수 있는지가 모두 SRS
  작성 시점에는 불확실했다 — 구현 전 실사 확인이 필요한 불확실한 외부 스펙이 있다는
  `CLAUDE.md` 3.1.1 기준에 해당한다.
- **Phase 0 스파이크 결과, 실제로 당초 예상과 다른 근본적인 사실이 드러났다**: 접근성
  API 기반의 합성 클릭/키 입력(System Events)만으로는 이 앱을 안정적으로 자동화할 수
  없고, `cliclick`(외부 도구)로 실제 HID 이벤트에 가까운 합성 이벤트를 보내야만 방을
  열고 메시지를 붙여넣고 전송하는 동작이 실제로 동작한다. 이는 SRS 8절/10절에 반영했다.
- 전송 검증 방식(SRS FR-12)이 스파이크 결과에 따라 바뀔 수 있어, 검증 로직에 의존하는
  이후 단계(다중 방 처리, 예외 처리)를 스파이크보다 먼저 구현할 수 없었다 — 실제로는
  당초 우려했던 "약한 검증으로 대체" 시나리오 없이, 원래 설계대로의 강한 검증(정확 일치)이
  가능함이 확인됐다.

## 2. Phase 개요
| Phase | 목표 | 관련 요구사항(SRS FR-ID) | 완료 기준 | 상태 |
|---|---|---|---|---|
| Phase 0 | 뼈대: UI 자동화 스펙 확인 + 하드코딩 단일 방 end-to-end 전송·검증 성공 | FR-1~FR-3(스파이크), FR-6~FR-12(스파이크로 방식 확정) | 하드코딩 방 이름 하나에 실제 카카오톡 메시지 1건이 전송되고 접근성 트리로 검증까지 성공 | **완료** |
| Phase 1 | Phase 0에서 확정한 방식을 CLI 골격 + 함수 단위로 정리(단일 방 기준) | FR-1~FR-3, FR-6~FR-12 | 단일 `--room`으로 정상/존재하지 않는 방/붙여넣기 확인 실패/전송 검증 실패 케이스가 SRS 7절대로 동작 | 예정 |
| Phase 2 | frontmost 안전장치 전면 적용 + 앱 레벨 예외(FR-4, FR-16) + 여러 방 순차 처리 + 결과 집계/종료 코드 | FR-4, FR-5, FR-13, FR-14, FR-15, FR-16, SRS 7절 예외 처리 전체 | SRS 9절 테스트 시나리오(부분 실패 포함) 전체 통과 | 예정 |
| Phase 3 | mock 기반 스모크 테스트 + `skill.yaml`/README(cliclick 설치 안내 포함)/CHANGELOG | SRS 9절 | 테스트 통과, Hermes 등록 준비 완료 | 예정 |

## 3. Phase별 상세

### Phase 0: 뼈대 + UI 자동화 스펙 확인 — **완료 (2026-08-11)**
- 범위 및 실제 진행 경과:
  - 실제 macOS 환경에서 `osascript`/System Events로 KakaoTalk.app을 조작하며 UI 요소
    구조를 확인함. 채팅방 목록/검색 결과는 `AXTable`의 row로 접근 가능하고, 각 row의
    `AXCell` 하위 `AXStaticText`로 방 이름을 정확히 매칭할 수 있음을 확인.
  - **핵심 블로커 발견**: System Events의 `click`/`perform action "AXPress"`로는 방을
    열 수 없었다(더블클릭 시뮬레이션이 clickCount=2로 전달되지 않는 것으로 추정). 메뉴바,
    컨텍스트 메뉴(AXShowMenu), Return 키 등 대안도 모두 실패.
  - Homebrew가 설치되어 있지 않아 먼저 설치(사용자가 직접 터미널에서 진행, 관리자
    비밀번호 필요해 에이전트가 대신 실행 불가)한 뒤 `brew install cliclick` 진행.
  - `cliclick`의 더블클릭(`dc:x,y`)으로 방 열기 성공. 이후 검색창 포커스, 메시지 입력창
    포커스도 접근성 API 클릭만으로는 불안정해 `cliclick` 단일 클릭이 필요함을 확인.
  - **실제 오동작 2건 재현**: (1) 오래된 좌표로 더블클릭한 결과 의도한 테스트방이 아닌
    실제 다른 대화방이 열림 → 즉시 창 닫고 복구. (2) frontmost 확인 없이 진행하다 키
    입력이 KakaoTalk이 아닌 VS Code로 유입됨 → 사용자가 직접 확인해 무해함을 확인. 이
    두 사례를 계기로 "frontmost 안전장치"와 "방 열기 후 이름 재확인"을 SRS에 필수
    요구사항(FR-5, FR-7)으로 추가함.
  - 클립보드 붙여넣기: 셸에서 로케일 미설정 시 `pbcopy`가 한글을 깨뜨림을 발견 → Python
    `subprocess`에서 UTF-8 인코딩 명시로 해결. System Events의 `keystroke ... using
    {command down}` 기반 Cmd+V는 포커스가 확인된 상태에서도 간헐적으로 무시됨을 반복
    재현 → `cliclick`의 `kd:cmd t:v ku:cmd` 조합으로 대체해 안정적으로 성공.
  - 입력창 접근성 `value`가 신뢰할 수 없음을 발견(내용이 있어도 빈 값 보고) → 전체선택
    +복사 후 클립보드 재확인 방식을 붙여넣기 검증 수단으로 채택.
  - 전송: `AXPress`/`Return` 키 모두 입력창을 비우지만 실제 전송은 안 됨을 여러 차례
    재현(사용자가 실제 화면에서 "메시지가 안 보인다"고 반복 확인해줌). `cliclick`으로
    전송 버튼을 클릭(마우스 이동 후 클릭)해서야 실제 전송 성공.
  - 전송 검증: 대화 내역 `AXTable`의 row에서 상대방 메시지, 내가 보낸 메시지 모두 원문
    텍스트를 읽을 수 있음을 확인 — 멀티라인 메시지(3줄)도 줄바꿈이 유지된 채 전송·검증
    성공, 사용자가 실제 화면으로 최종 확인함.
  - 확인 결과를 SRS 3절(FR 전면 재정의)/6절(흐름)/8절(외부 의존성)/10절(제약사항)에 반영함.
- 이번 Phase에서 제외한 것(다음 Phase로 미룸): 여러 방 처리, 방 검색의 중복 처리 코드화,
  화면 잠금/Accessibility 권한 거부 케이스의 정확한 에러 형태 확인, `src/` 코드 골격
  자체는 아직 작성하지 않음(스파이크는 대화형으로 셸 명령을 직접 실행하며 진행함)
- 완료 기준(Definition of Done): 하드코딩된 방 이름("ChatBotTest")으로 실제 카카오톡
  메시지가 전송되고, 접근성 트리 읽기로 전송 검증까지 성공 — **달성**. UI 요소 식별
  방법/붙여넣기 결과/전송 검증 가능 여부가 SRS에 반영됨 — **달성**.
- 이번 Phase는 대화형으로 셸 명령을 직접 실행하며 진행해 `CLAUDE.md` 3.2 기준의 "커밋
  단위"로 나누지 않았다. 코드 골격은 Phase 1에서 파일로 작성하며 그때부터 커밋을 나눈다.

### Phase 1: Phase 0 방식을 코드로 정리(단일 방)
- 범위:
  - `skills/kakaotalk_sender/`에 `src/`, `tests/`, `requirements.txt` 골격 생성
  - Phase 0에서 확정한 방식을 함수 단위로 옮긴다: `cliclick` 존재 확인(FR-3), 앱 활성화
    (FR-4 중 activate 부분), 방 검색(FR-6), 방 열기+이름 재확인(FR-7), 클립보드 설정
    (FR-8), 붙여넣기(FR-9), 붙여넣기 확인(FR-10), 전송(FR-11), 전송 검증(FR-12)
  - CLI 골격 작성: `argparse`로 `--room`(반복), `--message`/`--message-file`(상호배타)
    파싱 및 유효성 검증(FR-1), macOS 여부 확인(FR-2)
  - 위 함수들을 하나로 묶어 "단일 방에 메시지 보내고 결과(성공/실패, 실패 사유) 반환"
    하는 함수 완성 (frontmost 안전장치는 Phase 2에서 전체 적용 — 이번 Phase는 정상
    경로 위주로 실제 동작 재현에 집중)
- 이번 Phase에서 제외하는 것: 여러 방 순회, frontmost 안전장치의 전면 적용(FR-5),
  앱 레벨 예외(로그인 안 됨/권한 거부/화면 잠금), 결과 집계/종료 코드
- 완료 기준(Definition of Done): 단일 `--room` 인자로 정상 케이스가 실제 환경에서
  성공하고, 존재하지 않는 방/붙여넣기 확인 실패/전송 검증 실패 케이스가 각각 SRS 7절대로
  동작(가능한 범위에서 재현)
- 커밋 단위:
  1. `kakaotalk_sender: 기본 골격(src/tests/requirements.txt) 구성`
  2. `kakaotalk_sender: 대화방 검색(정확 일치/중복·미존재 처리) 구현`
  3. `kakaotalk_sender: 클립보드 붙여넣기(cliclick) + 붙여넣기 확인 구현`
  4. `kakaotalk_sender: cliclick 기반 전송 + 전송 검증 구현`

### Phase 2: frontmost 안전장치 + 앱 레벨 예외 + 다중 방 처리 + 결과 집계
- 범위:
  - **frontmost 안전장치(FR-5)를 Phase 1에서 만든 모든 상호작용 단계 직전에 적용** —
    Phase 0에서 실제로 재현된 오동작(VS Code로 키 입력 유입)을 코드 레벨에서 방지하는
    핵심 안전장치이므로 이번 Phase의 최우선 항목으로 다룬다
  - 앱 활성화 확인 로직 보강: 로그인 안 됨(메인 창 없음), Accessibility 권한 거부, 화면
    잠금 등을 구분해 감지하고 완전 실패로 처리(FR-4, FR-16) — 화면 잠금/권한 거부의
    정확한 에러 형태는 이번 Phase에서 실제로 재현해 확인한다
  - `--room` 여러 개를 순차 처리(동시 실행 없음), 방마다 Phase 1에서 만든 단일 방 처리
    함수를 재시도 없이 1회만 호출(FR-13)
  - 방별 성공/실패를 집계해 요약 로그 출력(메시지 원문은 남기지 않음, FR-15), 하나라도
    실패하면 0이 아닌 종료 코드 반환(FR-14)
- 이번 Phase에서 제외하는 것: 없음(SRS 기능 요구사항 전체 충족이 목표)
- 완료 기준(Definition of Done): SRS 9절 테스트 시나리오 중 부분 실패/완전 실패/권한
  없음/frontmost 불일치 케이스를 포함해 전체가 SRS 7절 표대로 동작
- 커밋 단위:
  1. `kakaotalk_sender: frontmost 안전장치 전면 적용`
  2. `kakaotalk_sender: 앱 활성화/로그인/Accessibility 권한 확인 구현`
  3. `kakaotalk_sender: 여러 방 순차 처리 + 결과 집계/종료 코드 구현`

### Phase 3: 테스트 + 문서화
- 범위:
  - `subprocess`(osascript/cliclick 호출)를 mock 처리한 스모크 테스트 작성: 인자 검증,
    방 검색 결과 분기(성공/미존재/중복), 붙여넣기 확인 실패 분기, 전송 검증 실패 분기,
    다중 방 결과 집계/종료 코드 결정 로직
  - `skill.yaml` 작성(Hermes 등록용)
  - `README.md`(사용법, **`cliclick` 설치 안내(Homebrew 필요, Homebrew 자체가 없으면
    먼저 설치해야 함을 명시)**, 손쉬운 사용 권한 부여 방법, 다른 스킬에서 서브프로세스로
    재사용하는 방법 예시 포함), `CHANGELOG.md` 작성
- 이번 Phase에서 제외하는 것: 실제 카카오톡 전송을 포함하는 자동 테스트(환경 의존적이라
  범위 밖, 수동 검증으로 갈음 — SRS 9절, Phase 0에서 이미 1차 수동 검증 완료)
- 완료 기준(Definition of Done): mock 기반 테스트 통과, `skill.yaml`/README/CHANGELOG
  작성 완료(`CLAUDE.md` 4절 체크리스트 5~7단계 충족)
- 커밋 단위:
  1. `kakaotalk_sender: mock 기반 스모크 테스트 추가`
  2. `kakaotalk_sender: skill.yaml 작성 (Hermes 등록용)`
  3. `kakaotalk_sender: README(cliclick 설치 안내 포함)/CHANGELOG 작성`

## 4. 불확실 요소 / 선(先) 확인 필요 항목
| 항목 | 불확실한 이유 | 확인 방법 | 결과 |
|---|---|---|---|
| 카카오톡 Mac 앱의 접근성(Accessibility) UI 요소 구조(채팅방 목록/검색창/입력창 식별 방법) | 간접 정보(블로그 글 등)만 있고 실제 앱에 붙어 확인한 적이 없었다 | `osascript` + System Events로 실제 앱의 UI 요소 트리 탐색 | **해결(Phase 0)**: `AXTable` row 구조 확인, SRS 8절에 반영 |
| 클립보드 붙여넣기(Cmd+V) 시 멀티라인 텍스트의 줄바꿈 유지 여부 | 키 입력 시뮬레이션보다 안정적일 것이라는 이론적 판단만 있고 실측하지 않았다 | 실제 멀티라인 텍스트로 붙여넣기 테스트 | **해결(Phase 0)**: `cliclick` 기반 Cmd+V로 줄바꿈 유지 확인, 실제 발송·표시까지 확인 |
| 전송된 마지막 메시지가 접근성 트리에 텍스트로 노출되는지(FR-12 검증 방식의 전제) | 채팅 뷰가 커스텀 렌더링이면 접근성 텍스트가 없을 수 있다 | 실제 전송 후 System Events로 값 읽기 시도 | **해결(Phase 0)**: 노출됨을 확인. 상대방/내 메시지 모두 `AXTextArea`/`AXStaticText` value로 읽힘 |
| 입력창에 붙여넣기 가능한 메시지의 실질적 길이 상한 | 공식 문서가 없어 추정만 가능 | 다양한 길이의 텍스트로 실제 붙여넣기/전송 테스트 | **미해결** — Phase 0에서는 짧은 멀티라인(3줄)까지만 검증. 실제 AI 뉴스 리포트 수준의 긴 텍스트는 Phase 1~2에서 추가 검증 필요 |
| 화면 잠금/Accessibility 권한 거부 시 정확한 에러 메시지·코드 | 실제로 재현해 에러 형태를 확인한 적이 없다 | 권한 해제 상태와 화면 잠금 상태에서 실제 실행해 에러 형태 확인 | **미해결** — Phase 2에서 확인 예정 |
| (Phase 0에서 새로 발견) System Events의 `click`/`AXPress`/`keystroke`가 이 앱에서 안정적으로 동작하는지 | 당초 SRS 초안은 System Events만으로 충분할 것으로 가정했다(문서화되지 않은 가정) | 실제 앱에 대해 클릭/더블클릭/키 입력을 하나씩 시도 | **해결(Phase 0)**: 안정적으로 동작하지 않음을 확인, `cliclick` 도입으로 해결. SRS 8절/10절 갱신 |
| (Phase 0에서 새로 발견) 셸의 `pbcopy`가 한글 텍스트를 정상 처리하는지 | 로케일(LANG/LC_ALL) 미설정 환경에서 테스트하다 발견 | 로케일 설정 여부에 따라 `pbcopy`/`pbpaste` 왕복 테스트 | **해결(Phase 0)**: Python `subprocess`에서 UTF-8 인코딩 명시로 해결, SRS FR-8/8절에 반영 |

## 5. Phase 간 의존성
- 전체적으로 순차 진행이다: Phase 1은 Phase 0에서 확정한 UI 요소 식별 방법과 상호작용
  방식(cliclick 기반)에 그대로 의존하므로 Phase 0 완료 없이는 시작할 수 없었다(완료됨).
  Phase 2는 Phase 1의 "단일 방 처리" 함수를 감싸는 구조라 Phase 1 완료가 선행돼야 한다.
  Phase 3의 mock 테스트는 Phase 1~2에서 함수 단위로 분리된 로직이 있어야 작성 가능해
  마지막에 진행한다.
- Phase 2 내부에서 "frontmost 안전장치 적용"과 "다중 방 순차 처리"는 서로 독립적이라
  순서를 바꿔도 무방하지만, 안전장치가 없는 상태로 다중 방 처리를 먼저 만들면 오동작
  위험이 커지므로 안전장치를 먼저 적용하는 순서를 권장한다.

## 6. 중단 시 상태
- Phase 0까지만 완료 후 중단(현재 상태): 코드 골격이 아직 없고, 대화형으로 확인한
  방식과 결과만 SRS/Implementation Plan에 문서화되어 있다. 실사용 불가. 다른 스킬
  폴더를 참조하지 않으므로 다른 스킬에는 영향 없음(스킬 격리 원칙, `CLAUDE.md` 3.3).
- Phase 1까지만 완료 후 중단: 단일 `--room`을 CLI로 지정해 정상 전송이 가능하지만, 여러
  방 처리와 frontmost 안전장치·앱 레벨 예외 처리가 없어 오동작 위험이 있는 상태로만
  실사용 가능(권장하지 않음).
- Phase 2까지 완료: SRS 기능 요구사항 전체 충족, 안전장치 포함 실사용 가능하나 자동
  테스트/문서가 없어 Hermes 등록 전 단계.
- Phase 3까지 완료: SRS 요구사항 전체 충족, Hermes 등록 준비 완료.
- 각 Phase는 다른 스킬 폴더를 참조하지 않으므로, 어느 시점에 중단되어도 기존 스킬(
  `ai_news_telegram` 등)의 동작에는 영향을 주지 않는다.
