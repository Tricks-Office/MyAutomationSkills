# Changelog — marathon_finder

## [0.1.2]
- **기능 추가**: 텔레그램 발송에 성공하면 동일한 메시지를 카카오톡 "러닝97" 방에도 보조
  채널로 발송(FR-11). `ai_news_telegram`의 기존 카카오톡 연동과 동일한 패턴 — `CLAUDE.md`
  3.3(스킬 격리)에 따라 `kakaotalk_sender`의 코드를 직접 import하지 않고 서브프로세스로
  호출하며, best-effort로 설계해 실패해도 텔레그램 발송/DB 반영 결과나 종료 코드에는
  영향을 주지 않는다
- 이미 매주 월요일 05:00에 Hermes 크론(`마라톤정보-주간공유`)이 이 스킬을 실행하도록
  구성되어 있어, 별도 스케줄 설정 없이 다음 실행부터 바로 적용된다
- PRD/SRS 갱신(FR-11), 스모크 테스트 4개 추가(`send_kakaotalk_notification` 성공/실패/예외/
  임시파일 기록)

## [0.1.1]
- `ai_news_telegram`에서 겪은 HN API 응답 지연 → 타임아웃 → 스킬 실패 장애를 계기로 이
  스킬도 점검, marathonmate.store 목록 페이지 요청(무재시도 1회)이 실패하면 그 실행의
  한국 대회 전체가 누락되는 부분 데이터 손실 위험을 확인
- 목록 페이지 요청에 연결 오류/타임아웃/5xx 재시도 로직 추가 — 최대 3회 시도, 1초·2초
  지수 백오프. 4xx 응답은 재시도해도 결과가 같아 제외
- 목록 페이지 요청 타임아웃을 30초 → 45초로 상향 (대회 상세 페이지/Claude/텔레그램
  타임아웃은 그대로 유지)
- 대회 상세 페이지 요청(이미 폴백 있음), Claude API 호출, 텔레그램 발송은 재시도 대상에서
  의도적으로 제외 — 각각 폴백 존재, 중복 과금/중복 발송 우려
- PRD/SRS에 재시도 정책(SRS FR-10) 반영, 재시도 시나리오 테스트 5개 추가

## [0.1.0]
- 한국(marathonmate.store) 크롤링, 중국 남경(Claude web_search) 검색, DB upsert, 텔레그램 발송까지
  전체 흐름 구현
- 실제 사이트/API 호출로 end-to-end 검증 완료 (한국 24건 + 중국 1건 수집, 텔레그램 발송 성공)
- 스모크 테스트 추가 (`tests/test_main.py`)
